# ---- 核心 ----
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
redis>=5.0.0
cryptography>=44.0.0
pydantic-settings>=2.7.0
asyncpg>=0.29.0

# ---- LangChain / LangGraph（新安全 Agent 层）----
# 安全铁律：锁定可信版本，避开已知投毒版本
langchain>=1.3.0,<2.0.0
langgraph>=1.2.0,<2.0.0
langchain-openai>=0.3.0
langchain-core>=1.4.0,<2.0.0
langgraph-checkpoint-redis>=0.4.0   # 需 Redis Stack（RedisJSON + RediSearch）

# ---- 测试 ----
pytest>=8.0.0
pytest-asyncio>=0.25.0
httpx>=0.28.0
fakeredis>=2.26.0

# ---- 定时任务 ----
croniter>=2.0.0
